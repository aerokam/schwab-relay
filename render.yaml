services:
  - type: web
    name: schwab-relay
    env: node
    plan: starter
    buildCommand: ./render-build.sh
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: "false"
